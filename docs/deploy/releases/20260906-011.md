# 2026-09-06 灰度发布记录（flask-ik19-011）

## 冻结范围

- Git commit: `8192d5f8ede1f947bf6cdecb044ffdeb6df8ee2d`
- 远端：`origin/main` 已同步，`HEAD...origin/main = 0/0`
- Spec: `SPEC-AI-OUTPUT-20260906-01`
- 范围：受控 AI 展示类别、短文本门禁、服务器确定性 `display_groups` 投影和旧提案兼容。
- 未提交差异：`specs/active/FEAT-002-MULTI-FAMILY-CHILD-TENANT-ISOLATION.md`
  的用户既有草案；未进入后端白名单发布目录，也未提交或部署。
- Schema：生产库从 `20260905_0002` 升级到 `20260905_0003`；应用期望值和唯一 head 均为
  `20260905_0003`。

## 数据库

- 发布前备份：用户于 2026-09-06 确认已备份；备份 ID 未在本地发布证据中记录。
- Migration：PASS，`alembic upgrade head` 完成，`alembic current` 为
  `20260905_0003 (head)`，`alembic check` 无待生成操作。
- `.env`：只保存在本机、被 Git 忽略，权限为 `0600`；发布包未包含该文件。
- CRUD-only 应用账号拒绝 DDL：NOT_RUN，当前本地只有 migration 管理账号证据。
- 数据库公网入口关闭：PENDING_MANUAL；迁移完成后必须由云账号管理员关闭。

## 自动检查

| 检查 | 结果 |
|---|---|
| `uv sync --all-groups` / `npm install` | PASS；0 vulnerabilities |
| Python unit/integration/contract | PASS；155 passed / 2 skipped |
| 新结构化输出 targeted tests | PASS；48 passed |
| Ruff lint / format | PASS；167 files |
| mypy | PASS；54 source files |
| 前端 Node tests | PASS；59 passed |
| ESLint | PASS |
| 架构检查 | PASS |
| Mini Program static validation | PASS；12 pages / 465083 source bytes |
| 本地 Docker | PASS；UID 10001，live/ready 200，SIGTERM exit 0 |
| 发布包 / dry run | PASS；58 files / 692721 bytes；无 `.env`、文档、测试、前端或本地数据库 |

两项 Python skip 为既有外部 MySQL/环境条件型用例。生产 migration 已另行执行并记录，但未完成
CRUD-only 账号拒绝 DDL 的权限验证。

## 后端灰度

- 发布目录：`dist/cloud-release`
- 版本：`flask-ik19-011`
- 备注：`push-kids-20260906-011-8192d5f`
- 平台状态：`normal`（2026-09-06 10:46:19）
- 服务状态：`normal`
- 公网入口：关闭
- CLI 实时日志最终超时；依据独立 `version:list` / `service:list` 权威状态判定版本创建成功。
- `wx.cloud.callContainer` live/ready、真实 Ark、双账号家庭隔离和完整业务 smoke：NOT_RUN；本机锁屏
  阻止模拟器 UI 操作，不能用版本 `normal` 代替业务验收。
- 全量切流：NOT_RUN；保持灰度状态，等待上述验收和人工门禁。

## 前端本地状态

- 微信开发者工具登录：PASS。
- 本地开发预览：PASS；真实 AppID `wx5d2e0014690bf285`，包体 424.2 KB / 434389 bytes。
- 云环境配置：`useCloud=true`、环境 `prod-d2g14rwoycac6b45d`、服务 `flask-ik19`、`urlCheck=true`。
- 本次结构化列表前端：NOT_IMPLEMENTED；`DREV-20260906-AI-02` 的 Figma node、审批快照与三视口
  证据尚未完成，当前只部署了可连接新后端的既有前端。
- 微信开发版本上传、体验版、审核、正式版：均 NOT_RUN。

## 回滚与剩余门禁

- 后端回滚目标：`flask-ik19-010`；Schema 变更为 additive，事故中不执行 destructive downgrade。
- 必须先关闭数据库公网入口。
- 解锁本机后补做真实 AppID 的 live/ready 和完整业务 smoke。
- 前端需恢复 Figma 额度完成设计审批，或由产品负责人明确批准限定范围的一次性 waiver，之后才能
  实现结构化列表并做三视口原生验收。
