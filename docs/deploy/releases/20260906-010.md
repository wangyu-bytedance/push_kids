# 2026-09-06 灰度发布记录（flask-ik19-010）

## 冻结范围

- Git commit: `72970f35306626ba6e6c50862306e0f7e5240ec2`
- 远端：`origin/main` 已同步，`HEAD...origin/main = 0/0`
- 合并基线：远端前端样式提交 `3763b23` + 本地 BUG-011/BUG-012 运行时修复
- 未提交差异：`specs/active/FEAT-002-MULTI-FAMILY-CHILD-TENANT-ISOLATION.md`
  的未批准需求草案；未进入后端白名单发布目录，也未提交或部署
- Schema：无变更；Alembic head 与应用期望值均为 `20260905_0002`

## 自动检查

| 检查 | 结果 |
|---|---|
| `uv sync --all-groups` | PASS |
| `npm install` | PASS，0 vulnerabilities |
| Python unit/integration/contract | PASS，132 passed / 2 skipped |
| Python live HTTP E2E | PASS，1 passed |
| Ruff lint / format | PASS |
| mypy | PASS，53 source files |
| 前端 Node tests | PASS，59 passed |
| ESLint | PASS |
| 架构检查 | PASS |
| Mini Program static validation | PASS，11 pages / 247812 source bytes |
| 本地 Docker | PASS；UID 10001，live/ready 200，SIGTERM exit 0 |
| 微信开发者工具 preview | PASS；257.2 KB / 263344 bytes |
| 五个主 Tab 云数据 smoke | PASS；今日、日程、记录、报表、设置均加载，0 console errors |
| 视口 | PASS；320×568、390×844、430×932 |

两项 Python skip 为需要外部 MySQL/环境的既有条件型用例。首次本地容器尝试遗漏
`PUSH_KIDS_ENV=test` 而按设计启动失败；补齐受支持配置后重新验证通过。直接在 SQLite 上执行
MySQL 生产 migration 会因 `ALTER COLUMN` 失败，本地运行按既有 `Database.create_schema()` 路径初始化，
不把该错误尝试计为 migration 通过。

## 后端灰度

- 发布包：最小白名单目录 `dist/cloud-release`
- Manifest：57 files / 681880 bytes
- Dry run：PASS
- 版本：`flask-ik19-010`
- 备注：`push-kids-20260906-010-72970f3`
- 镜像 digest：`sha256:987879a803b852ad97597a7e2dd2fcee35564d9efa4774c445662ae9c0bd87f1`
- 平台状态：`normal`（2026-09-06 09:33:32）
- 服务状态：`normal`
- 公网入口：关闭
- CLI `TopicNotExist`：仅日志订阅异常；依据独立 `version:list` / `service:list` 判定成功

## 前端状态与后续发布阶段事项

- 本地开发预览已成功，真实 AppID 和云托管数据可加载。
- 未上传新的微信“开发版本”，未设置体验版，未提审，未发布正式版。
- NOT_RUN：iOS/Android 真机相册、相机、隐私授权与后台恢复。
- NOT_RUN：两个真实微信账号的家庭隔离、图片 owner、邀请/申请/审批全链路。
- NOT_RUN：灰度约两分钟的新旧版本共存指标与生产日志完整观察。
- NOT_RUN：生产 MySQL 空库/上一版本副本 migration；本次无 Schema 变更且无受控 MySQL runner。

因此 `flask-ik19-010` 的实际到达状态是“灰度不可变版本已创建且平台正常”；本次发布范围未包含
全量切流或前端上传。上述 `NOT_RUN` 项留给后续发布阶段复核，不表示本版本存在代码问题或 blocker。
