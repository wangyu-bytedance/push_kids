# 2026-09-06 灰度发布记录（flask-ik19-013）

## 冻结范围

- Git commit: `c698a09d7b4fc716c32d49ff226f6b6aaa2481b2`
- 发布开始时工作区干净；`main` 相对 `origin/main` ahead 1。
- 范围：出行安排、跨来源日程冲突、每孩子 20 项在用时段上限、持久化孩子/家庭数据删除、
  学龄前班级粒度和新家庭入口修复。
- 用户在本次 migration 前已确认数据库完成备份；本记录未取得控制台备份 ID。

## 数据库与自动检查

- 生产 migration：`20260906_0005 → 20260906_0006 → 20260906_0007 (head)`。
- `alembic current` 为唯一 `20260906_0007`；`alembic check` 无待生成操作。
- Python unit/integration/contract：PASS，200 passed / 2 skipped。
- Frontend Node：PASS，107 passed；ESLint 和 Mini Program static validation PASS。
- Ruff、format、Mypy、architecture：PASS；212 files formatted，67 source files typed，
  Mini Program 14 pages / 576887 source bytes。
- 本地 Docker：PASS；UID 10001，端口 8000，live/ready 200，SIGTERM 后容器正常退出。
- 发布包：PASS；71 files / 775706 bytes；无 `.env`、文档、测试、前端、本地数据库或压缩包。
- Cloud dry run：PASS。

## 后端灰度

- 版本：`flask-ik19-013`
- 备注：`push-kids-20260906-013-c698a09`
- 云端镜像 digest：
  `sha256:8321761c969c70f1413e89a41464488080fc1dfc0e4d0098105ff163f088ef2e`
- 镜像构建：PASS，`check_build_image = succ`。
- 平台状态：`normal`（2026-09-06 15:54:00）。
- 服务状态：`normal`；PUBLIC 公网入口关闭。
- CLI 的 `TopicNotExist` 只影响实时日志订阅；最终状态由独立 `version:list` 和
  `service:list` 确认。
- 发布范围：灰度版本已创建；未执行全量切流。回滚目标为 `flask-ik19-012`。

## 前端本地预览与上传

- 微信开发者工具登录、打开项目及本地预览编译：PASS。
- 上传版本：`0.1.2`。
- AppID：`wx5d2e0014690bf285`。
- 说明：`新增出行安排与日程冲突提示，优化孩子档案、家庭入口和数据清理`。
- 上传包体：518.5 KB / 530964 bytes。
- 状态：开发版本已上传；尚未由管理员选为体验版，未提交审核，未发布正式版。

## 待人工/体验验证

- 真实 AppID 的 `wx.cloud.callContainer` live/ready：NOT_RUN；本机系统录屏服务无法捕获开发者工具
  界面，平台版本和服务状态不能替代该业务链路验证。
- 两个真实微信账号的家庭隔离、图片 owner、metaid 解码与公网拒绝：PASS（2026-09-06，用户在
  发布后的体验验证中确认）。
- AI、确认、出行冲突、20 项上限、数据删除和完整业务 smoke：NOT_RUN，仍交由体验版执行。
- iOS / Android、320 / 390 / 430 视口、弱网、后台恢复和日志指标观察：NOT_RUN。
- 后端全量切流必须在体验证据通过后由发布负责人在控制台确认。
