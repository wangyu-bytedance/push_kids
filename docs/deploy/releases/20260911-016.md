# 2026-09-11 发布记录（flask-ik19-018 / 小程序 0.1.5）

## 冻结范围

- Source Git commit: `1658c26240bef0b81d392aa3ccccfa8815c49005`。
- Scope: 已批准的 `BUG-017`、`BUG-018`、`FEAT-009` 后端范围及 2026-09-11 已批准前端修订；
  明确不包含尚未批准的 FEAT-002 多家庭草案和 FEAT-008 学习空档提醒草案。
- Release package: 85 files / 954219 bytes。
- Manifest SHA-256: `508ad8e9bc4638347be548170eefba485d2c33011b12f640e39078f3bfb71f1e`。
- Package source: detached clean worktree at the frozen source commit; allowlist generation and secret scan passed。
- Cloud dry run: PASS；发布包未包含 `.env`、`.git`、文档、测试、前端、本地数据库、图片或日志。
- Cloud deploy upload boundary: PASS；实际上传/解压日志仅出现 manifest 内的后端源码、Dockerfile、
  `pyproject.toml`、`uv.lock` 和 `wxcloud.config.json`。

## 数据库与自动检查

- Schema target: `20260911_0011 (head)`；生产发布前 revision：`20260907_0009`；生产发布后
  revision：`20260911_0011`。
- Production backup: PASS；operator confirmed backup completed before production migration.
- Production MySQL read-only check: PASS；通过受控本地环境连接 `push_kids`，`alembic_version`
  初始为 `20260907_0009`。
- Production migration: PASS；`20260907_0009 -> 20260911_0010 -> 20260911_0011` 已执行；
  两张 weekday slot 表、唯一约束和 tenant-leading read indexes 已创建，slot 数据已回填
  `activity_schedule_slots=5`、`travel_arrangement_slots=20`。
- Production `alembic check`: PASS after MySQL 5.7 compatibility handling. Tencent CynosDB reports
  `5.7.18-cynos-2.1.14-log` and does not expose/enforce MySQL 8 CHECK constraint metadata, so Alembic
  now skips CHECK constraint comparison only for MySQL versions below `8.0.16`; runtime/service-layer
  validation remains the source of truth for weekday and time-range validity on this production engine.
- Fresh local MySQL 8.0.45: base-to-head、`current`、`check`、`0011 -> 0010 -> 0011` 回演 PASS；
  4 项 MySQL 集成/执行计划测试 PASS。
- Backend unit/integration/contract: 298 passed；performance: 2 passed。
- SQLite comparable scale: dashboard 10k 137.5 ms / 13 SQL / 5327 bytes；report 10k 的 7/30/100 天
  83.8/86.8/103.1 ms / 8 SQL / <=14020 bytes；history 50k 首屏 18.1 ms、substring search 87.7 ms。
- Frontend: 144 passed；ESLint PASS。
- Ruff、format、Mypy（81 source files）、architecture（checked=3）、Mini Program validator
  （15 pages / 632049 source bytes）、empty runtime DB audit、diff check：PASS。
- `tools/gen_tabbar_icons.py --check`: NOT RUN；当前 Python 环境缺少可选 `cairosvg`，已检入图标未变化，
  Mini Program validator 通过。
- Local Docker image build: BLOCKED by build-network download；多次均在 `pip` 下载 19.4 MB `uv` wheel
  后长时间无进展并被人工终止。2026-09-11 追加探针显示宿主机 `pip download uv==0.12.13`
  25 秒超时，容器内同样无进展；基础镜像、Dockerfile 和 960.81 kB build context 均已成功读取。
  这不是容器 smoke 通过证据，部署前仍需补跑 image/UID/live/ready/graceful-stop。
- Local Docker gate override: 2026-09-11 operator accepted skipping local image build/smoke for this release
  because current `wxcloud run:deploy dist/cloud-release ... --dockerfile Dockerfile --targetDir .` uploads
  source for cloud-side image build. Backend release must compensate with cloud build log review, immutable
  version status, and cloud live/ready verification before any frontend upload or traffic expansion.

## 前端预览

- WeChat DevTools login: PASS。
- Registered AppID preview: PASS，AppID `wx5d2e0014690bf285`。
- Preview package: 563.7 KB / 577274 bytes。
- Upload: PASS；小程序开发版本 `0.1.5` 已上传，说明：
  `优化历史、今日和报表加载，完善记录入口、导航与按星期日程时间`。
- Uploaded package: 563.7 KB / 577274 bytes。
- State: 开发版本已上传；未设置体验版、未提交审核、未发布正式版。

## 当前发布状态与下一门禁

- Backend: 灰度不可变版本已创建，`flask-ik19-018` 为 `normal`；release type `GRAY`；
  remark `push-kids-20260911-016-1658c26`。
- Cloud image: `flask-ik19-018-20260911233919`，digest
  `sha256:9765240c074b4d017ab02390c3f204fc398616659114b4c113e713ba8252d631`。
- Cloud service: `flask-ik19` 为 `normal`，公网访问保持关闭。
- Cloud live/ready via real `wx.cloud.callContainer`: NOT_RUN；公网关闭且当前仓库无 CLI 可替代
  MINIAPP 私有入口的自动 smoke 脚本，需在体验版/真机链路补验。
- Database: 已迁移到 `20260911_0011`。
- Frontend: 开发版本 `0.1.5` 已上传；必须由管理员在微信公众平台选为体验版后再进行 iOS/Android、
  第二家庭、图片 owner、AI 可编辑确认、历史/今日/报表等真机 smoke。
- Experience/review/production: NOT RUN。
- Rollback target: 后端可切回上一已验收不可变版本 `flask-ik19-015`；数据库保持 additive schema，
  事故中不做破坏性 downgrade；保留生产备份直到观察窗口结束。
