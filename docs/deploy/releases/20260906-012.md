# 2026-09-06 灰度发布记录（flask-ik19-012）

## 冻结范围

- Git commit: `19bd937320412ebd14d1800253783e6b7499cd6c`
- 远端：`origin/main` 已同步，`HEAD...origin/main = 0/0`
- 合并范围：结构化 AI 输出、多科目分别入档与确认动线修复、多孩子档案生命周期，以及
  MySQL 外键索引迁移顺序修复。
- 未提交差异：`specs/active/FEAT-002-MULTI-FAMILY-CHILD-TENANT-ISOLATION.md`
  的用户既有草案；未进入白名单发布目录、提交或镜像。
- Schema：应用期望值、Alembic 唯一 head 和生产数据库均为 `20260906_0005`。

## 数据库与自动检查

- 用户确认发布前已备份。
- 首次 `0004` 生产 migration 被 MySQL 外键索引规则拒绝，revision 保持 `0003`；修正为先创建
  替代索引再删除唯一索引，并增加顺序回归测试后重试成功。
- 最终 migration：`20260905_0003 → 20260906_0004 → 20260906_0005 (head)`；
  `alembic check` 无待生成操作。
- Python unit/integration/contract：PASS，186 passed / 2 skipped。
- Frontend Node regression：PASS，91 passed；ESLint PASS；Mini Program static validation PASS，
  13 pages / 552772 source bytes。
- Ruff、format、mypy、architecture：PASS，180 files formatted，55 source files typed。
- 本地 Docker：PASS；UID 10001，live/ready 200，SIGTERM exit 0。
- 发布包：PASS；59 files / 723533 bytes；无 `.env`、文档、测试、前端或本地数据库。
- Cloud dry run：PASS。

## 后端灰度

- 版本：`flask-ik19-012`
- 备注：`push-kids-20260906-012-19bd937`
- 云端镜像 digest：
  `sha256:afc94a5c35e013dc456544d284d38210cb1f5fc44c04976473e3d1c1283e1609`
- 镜像构建：PASS，`check_build_image = succ`。
- 平台状态：`normal`（2026-09-06 14:40:14）。
- 服务状态：`normal`。
- PUBLIC 公网入口：关闭。
- CLI 的 `TopicNotExist` 只影响实时日志订阅；最终状态由独立 `version:list` 和
  `service:list` 确认。
- 发布范围：灰度版本已创建；未执行全量切流。

## 尚未完成的生产验收

- 真实 AppID 的 `wx.cloud.callContainer` live/ready：NOT_RUN；本机 UI 捕获服务失败。
- 两个真实微信账号的家庭隔离、图片 owner、AI、确认、多科目入档和多孩子档案业务 smoke：
  NOT_RUN。
- 灰度共存指标、5xx/429、延迟、Job queue 和脱敏日志观察：NOT_RUN。
- 数据库公网入口关闭状态：未从控制面复核，仍需管理员确认。

因此本记录只声明 `012` 不可变灰度版本已创建且平台状态正常，不声明已全量发布。
回滚目标为 `flask-ik19-011`；Schema 为 additive，事故中不执行 destructive downgrade。
