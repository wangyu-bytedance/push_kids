# 2026-09-07 灰度发布记录（flask-ik19-015）

## 冻结范围

- Git commit: `d38065ad4ecf8a6371ebb7cb907e1557991a29f3`（`main` 与 `origin/main` 一致）。
- Scope: PR #6 的数据删除/无家庭流程和通知通道，以及 `BUG-SPEC-20260907-22` 的通知删除归属、
  purge、冻结后禁止重新入队/发送修复。
- Release package: 83 files / 895400 bytes。
- Manifest SHA-256: `e946edd6b228b51be9e7f44ad70e258e7cc579e6a536b31925e53ee9ea2d9203`。
- Cloud dry run: PASS；发布包未包含 `.env`、测试、文档、前端、本地数据库或儿童图片。

## 数据库与自动检查

- 用户确认备份成功后，生产 MySQL 从 `20260906_0007` 顺序升级到 `20260907_0009 (head)`。
- 生产 MySQL `alembic current` PASS；`alembic check` PASS，`No new upgrade operations detected`。
- Backend full test: 264 passed / 2 skipped；通知删除专项：24 passed。
- Ruff、format、mypy、architecture：PASS。
- Frontend test: 124 passed；ESLint、Mini Program validator：PASS（15 pages / 628174 source bytes）。
- 本地容器 smoke：在 `c6f4f5a` 运行时等价包上 PASS（UID 10001，`/health/live` 与
  `/health/ready` 返回 200）；最终 `d38065a` 只补 ORM 既有索引声明，未重新构建本地镜像。

## 后端灰度

- Version: `flask-ik19-015`。
- Remark: `push-kids-20260907-015-d38065a`。
- Image digest: `sha256:d453e96b5e807cbaaa098010da996e9bde36a1fa280ef7a4848258a63d10ee83`。
- Image build: PASS，`check_build_image = succ`。
- Platform status: `normal`（2026-09-07 11:03:02）。
- Service status: `normal`；PUBLIC 公网入口关闭。
- Release scope: 灰度版本已创建，未执行全量切流；回滚目标为 `flask-ik19-014`。
- CLI 的 `TopicNotExist` 仅影响实时日志订阅，最终状态由独立 `version:list` / `service:list` 确认。

## 前端预览与上传

- 微信开发者工具登录、预览与上传：PASS。
- Version: `0.1.4`。
- AppID: `wx5d2e0014690bf285`。
- Description: `接入提醒与永久删除，修复删除期间通知入队和发送边界`。
- Package: 562.1 KB / 575557 bytes。
- State: 开发版本已上传；尚未选为体验版、提交审核或发布正式版。

## 待体验与人工门禁

- 真实 AppID 的 `wx.cloud.callContainer` live/ready：NOT RUN；公网关闭且 CLI 无法替代小程序私有入口。
- 真实 child/family 删除、通知设置/授权/入队/发送、两家庭隔离与私有媒体删除：NOT RUN。
- iOS / Android、320 / 390 / 430 原生视口与弱网/后台恢复：NOT RUN。
- 体验验证通过前不执行后端全量切流，不提交微信审核或正式发布。
