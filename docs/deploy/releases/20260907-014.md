# 2026-09-07 灰度发布记录（flask-ik19-014）

## 冻结范围

- Git commit: `a50a9678f603ed639c5465e3435d70c0b527fe39`
- 发布开始时工作区干净，`main` 与 `origin/main` 一致。
- 范围：PKDS-2.0「纸 · 芽」前端升级、记录页交互优化、报表指标七点趋势、归档孩子档案写入边界修复，
  以及结构化增量分析稳健性修复。
- 明确不包含 PR #6、通知页和 `20260907_0008` migration。

## 数据库与自动检查

- 生产 Schema 发布前后均为唯一 `20260906_0007 (head)`；本次没有数据库 migration。
- 生产 MySQL `alembic current` 与代码 head 一致，`alembic check` 无待生成操作；核验完成后公网入口已
  关闭并通过连接超时确认。
- Python unit/integration/contract：PASS，213 passed / 2 skipped。
- Frontend Node：PASS，112 passed；ESLint 和 Mini Program static validation PASS。
- Ruff、format、Mypy、architecture：PASS；226 files formatted，68 source files typed，
  Mini Program 14 pages / 607857 source bytes。
- 本地 Docker：PASS；UID 10001，端口 8000，live/ready 200，SIGTERM 后容器正常停止。
- 发布包：PASS；72 files / 794264 bytes；无 `.env`、文档、测试、前端、本地数据库、图片、日志或压缩包。
- 发布 manifest SHA-256：
  `e2baf29273795863f1d027a5ba5d4f3127466568fc6b9582af528df969da0637`。
- Cloud dry run：PASS。

## 后端灰度

- 版本：`flask-ik19-014`
- 备注：`push-kids-20260907-014-a50a967`
- 云端镜像 digest：
  `sha256:b9538d555271dabad2cea638771461d0628aa25343df0e2f3b7f41978dcb619c`
- 镜像构建：PASS，`check_build_image = succ`。
- 平台状态：`normal`（2026-09-07 00:33:45）。
- 服务状态：`normal`；PUBLIC 公网入口关闭。
- CLI 的 `TopicNotExist` 只影响实时日志订阅；最终状态由独立 `version:list` 和
  `service:list` 确认。
- 发布范围：灰度版本已创建；未执行全量切流。回滚目标为 `flask-ik19-013`。

## 前端本地预览与上传

- 微信开发者工具登录及本地预览编译：PASS。
- 上传版本：`0.1.3`。
- AppID：`wx5d2e0014690bf285`。
- 说明：`升级纸芽界面，优化记录流程与报表七点趋势，修复归档档案写入边界`。
- 上传包体：546.9 KB / 560059 bytes。
- 状态：开发版本已上传；尚未由管理员选为体验版，未提交审核，未发布正式版。

## 待人工/体验验证

- 真实 AppID 的 `wx.cloud.callContainer` live/ready：NOT_RUN；CLI 无法直接调用 MINIAPP 专用入口，
  平台版本和服务状态不能替代该业务链路验证。
- 两个真实微信账号的家庭隔离、图片 owner、AI、确认、归档孩子收尾写入和完整业务 smoke：NOT_RUN，
  交由体验版执行。
- iOS / Android、320 / 390 / 430 视口、弱网、后台恢复和日志指标观察：NOT_RUN。
- 后端全量切流必须在体验证据通过后由发布负责人在控制台确认。
