# Behavior catalog — FEAT-001 / FEAT-002 / FEAT-005 / FEAT-007

| ID | Behavior | Owner | Verification |
|---|---|---|---|
| `BHV-001` | Parent creates children and learning/activity subjects | children | API integration tests |
| `BHV-002` | Parent uploads photos or enters text with occurrence time; empty interrupted drafts can resume/cancel, timestamps round-trip in UTC, pending records are filtered before pagination | learning + persistence | P2 boundary and page logic tests |
| `BHV-003` | Analysis uses strict subject-grouped JSON Schema plus local reference/domain validation; invalid model output regenerates at most twice and becomes an actionable terminal error without persisting raw output, while ordinary transport/loop faults back off and recover | agent_processing + bootstrap | grouped provider contract, worker fault-injection and readiness contract tests |
| `BHV-004` | Only parent confirmation of a server-resolved learning subject creates formal learning/knowledge/review data; activity rejection retains the draft without writes | learning + knowledge + planning | transaction and activity-boundary tests |
| `BHV-005` | AI proposals omit same-occurrence-day confirmed duplicates; a due/overdue current Todo with direct evidence is proposed as Review instead of new learning, and confirmation advances it once without creating a LearningRecord/Occurrence; stale snapshots fail atomically with `review_state_changed` | knowledge + learning + agent_processing | grouped analysis unit, AI reliability and learning-flow integration tests |
| `BHV-006` | Review schedule follows deterministic memory-curve policy for learning subjects; activity reviews never enter Todo, practice materials or feedback | planning | policy unit and historical activity-boundary tests |
| `BHV-007` | Historical backfill produces a current check, not past Todo debt | planning | backfill unit test |
| `BHV-008` | Daily Todo is grouped under a time budget and accepts four feedback types; closed reviews reject new feedback while successful idempotent requests replay their result | planning | service and P2 boundary tests |
| `BHV-009` | Activity schedules suggest practice and show last practice without entering the memory curve | activities | integration and activity-boundary tests |
| `BHV-010` | Dashboard/report/calendar summarize confirmed evidence; report overview totals and its seven contiguous `Asia/Shanghai` trend buckets share the same family/child-scoped source and preserve totals for 7/30/100 days; overview cards are read-only, while missing/invalid trend fields degrade to “暂无趋势” and observed zero remains a real zero line; review statistics exclude historical activity reviews while activity records and historical learning evidence remain available | reporting + Mini Program | reporting trend policy/integration, activity-boundary and frontend trend contract tests; native viewport acceptance pending |
| `BHV-011` | Every resource is isolated by family scope | platform | cross-family tests |
| `BHV-012` | Cloud requests derive family scope from a bound WeChat actor and reject client family authorization; ordinary Mini Program launch resolves `/me` before business tabs and maps unbound/pending/bound/error without treating an empty child list as no family | platform + families + Mini Program | cloud identity + family onboarding lifecycle + real two-account tests |
| `BHV-013` | Cloud images use server-issued private upload paths and are claimed only after uploader/path/content validation | media + learning | adapter tests + real storage E2E |
| `BHV-014` | Analysis uses images/text, configured learning subjects, provided grade, balanced history, complete bounded same-day confirmed items and only currently due/overdue Review candidates; repeated materials warn without auto feedback and all evidence remains parent-confirmed | agent_processing | context / grouped provider contract and reliability tests; live model quality not verified |
| `BHV-015` | Manual confirmation works during queued/analyzing/failed/pending states; saves atomically and cancels unfinished work; stale provider success/failure cannot overwrite terminal states | learning + agent_processing | manual state / invalid input / late-result integration and page tests |
| `BHV-016` | Settings shows three base learning subjects and only explicitly added other subjects/activities; opening or cancelling a catalog/reminder editor performs no write | children + activities + Mini Program | settings page state and activity integration tests |
| `BHV-017` | A family invite approves at most one applicant; approval/revocation expires sibling pending requests and releases pending bindings; invite lists omit raw tokens | families | family onboarding integration tests |
| `BHV-018` | Member management rejects self-removal at the service boundary and keeps role changes/removal of other members behind manager authorization and confirmation | families | role/self-removal integration and frontend state tests |
| `BHV-019` | History searches/paginates confirmed and pending submissions without duplicating confirmation; reused detail exposes confirmed evidence, original media and current review feedback without business writes | learning + reporting + planning | PASS：learning_history integration、learning-history/submission-materials page tests，以及 2026-09-06 用户确认的原生/云端验收 |
| `BHV-020` | Original media preview validates family/child/submission/media membership, permits viewers, rejects removed members and cancelled media; cloud GET capabilities expire within 60 seconds | learning + media | PASS：viewer/isolation/cancellation、signing/limiter，以及 2026-09-06 用户确认的真实云端双参与者 owner/metaid 验收 |
| `BHV-021` | Todo items carry structured provenance (`source_submission_id`、`source_occurred_on`、`review_round`、`interval_days`) computed by deterministic policy; missing provenance degrades to `null` instead of hiding the Todo. Photos keep a stable `sort_order`; confirmed knowledge keeps machine recognition `confidence`/`evidence` (recognition reliability only, never mastery); subjects record preset vs parent-built origin | planning + learning + children | planning domain unit, todo provenance / migration backfill integration and subject contract tests |
| `BHV-022` | One photo batch spanning several subjects is split by deterministic server-side subject routing (separator/conjunction/stopword split plus catalogue alias matching), producing one confirmed learning record per subject; merged labels such as `数学、语文` are never created, and a subject outside the child's catalogue requires explicit parent consent (`409 consent_required`) before it exists | agent_processing + learning + children | subject routing unit, multi-subject confirmation and migration integration tests, draft grouping page tests; live model subject accuracy not verified |
| `BHV-023` | A family keeps several child profiles in use (at most five, names unique among profiles in use); archiving a profile removes it from switchers and rejects new learning/subject/activity writes with an explicit message (in-flight completion boundary in `BHV-025`), while dashboards, reports, history and original media stay readable; archiving/restoring is idempotent, releases and re-checks the name, is manager-only and is recorded in the family audit trail on the verified-identity path | children | multi-child profile integration, contract and migration tests |
| `BHV-025` | Archiving a child profile blocks new intake only (new submission, photo draft, subject, activity, schedule → 409), while work that already exists may still be finished: a draft created before archiving can be confirmed, an existing review item still accepts feedback, an already uploaded batch still finishes analysis/retry, and profile fields stay editable; the Mini Program shows how many records are still unfinished (pending + analyzing + failed) before archiving, degrades to plain copy when that count cannot be read, and never blocks archiving | children + learning + planning + Mini Program | `test_archived_profile_finishes_work_that_started_before_archiving` and `tests/frontend/child-profiles.test.js`; real two-device concurrency not verified |
| `BHV-024` | Opening a family no longer requires a child; a new profile may seed the subjects the parent picked, and every surface resolves the current child through one shared context that falls back to the first profile in use when the selected one is archived or gone | children + Mini Program | multi-child profile integration and `tests/frontend/child-profiles.test.js` |
| `BHV-025` | A parent can manage child-scoped weekly travel arrangements in Settings; they project only into Calendar, where every overlapping timed item is marked with structured conflict details under the half-open interval rule, while touching endpoints remain non-conflicting | travel + activities + Mini Program | travel CRUD/migration integration, calendar conflict unit, and frontend travel/calendar tests; native viewport/device acceptance pending |
| `BHV-026` | Notifications are best-effort and never claimed falsely: three kinds (`member_application` to managers, `schedule_reminder` to the whole family 60 minutes before a schedule naming child/time/activity, `review_digest` to the whole family at 19:00 Asia/Shanghai listing what is still unreviewed) are queued only for a member who both switched the kind on and holds a spendable WeChat grant. A dedupe key owns one row, an edited schedule refreshes it in place, a deleted schedule or a decided application cancels it, transient provider faults back off within bounded retries, repeat accepts of a one-off template accumulate spendable quota while a per-dialog refusal keeps quota already granted and an explicit WeChat ban zeroes it, a spent one-off grant is reflected in the stored grant, and every terminal row keeps a result code the member can see. Message content is produced only from stored facts and clamped to the limit of each WeChat field type, and a template slot the deployment mapped but the content cannot fill is skipped as `template_field_missing` before WeChat is called — so a misconfigured template never spends a member's one-off grant and the reminder can be re-armed once the mapping is fixed | notifications | notification policy unit, channel / resilience / lifecycle / migration integration and API contract tests; live WeChat send not verified |
| `BHV-027` | The channel is revocable and private: the WeChat receiver is stored AES-GCM encrypted with a key version and is never logged, plaintext or indexed; business time is computed server-side and never read from the client; leaving the family revokes the grant and receiver and cancels in-flight messages, membership is re-checked immediately before sending, an unavailable channel refuses to store a grant it could never spend, and terminal delivery history is pruned after 30 days while in-flight rows are never pruned | notifications + platform | `tests/integration/test_notification_channel_lifecycle.py`, `tests/integration/test_notification_resilience.py`, `tests/unit/test_notification_policy.py` |
| `BHV-028` | Parents manage reminders from 设置 → 提醒设置, where willingness and capability are shown as two separate facts: a per-kind switch records whether the member wants the reminder, while the WeChat grant state records whether it can still be delivered. The grant is requested inside the user's own tap without any awaited request in front of it, at most three templates per call, and only an explicit `accept` is reported back as authorized while WeChat's own `reject`/`ban`/`filter` wording is passed through so a single declined dialog does not destroy quota the parent already paid for; because the WeChat console offers one-off templates only, the page states how many reminders are still spendable and offers a top-up that re-asks for the kinds with the least quota first; an unavailable channel or an unsupported WeChat client asks for nothing and explains itself; a failed preference write rolls the switch back instead of showing a false `已开启`; recent deliveries list every result — including failures with a reason the parent can act on — and a failed history read degrades only that section | Mini Program + notifications | `tests/frontend/notifications.test.js`, `tools/validate_miniprogram.py`; WeChat DevTools and real-device grant not verified |
