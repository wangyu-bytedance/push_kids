# BUG-010 截图回归与二级交互提案

日期：2026-09-05。状态：UX-04 原生视觉已在 390×844 完成六页同尺寸对照，整体仍 VERIFYING，未部署。

依据：用户 20:52–20:53 的五张截图。原生修复属于已批准 BUG-010 / UX-03 的控件几何、七日完整展示和报表组件分页范围；UX-04 新视觉与二级交互仍为设计草稿，两者不混同。

## 原生修复

| 问题 | 本地修复 | CUA 证据 |
|---|---|---|
| 科目与活动目录按钮重叠、右侧截断 | 用容器限定选择器约束原生按钮宽度和最小宽度；名称与“已添加”分行 | 390×844 两个弹层可完整阅读；320×568 科目弹层通过 |
| 星期文字贴上边 | 等宽高、flex 双轴居中、保留 1:1 比例约束 | 390×844 安排弹层通过 |
| 日历左侧留白、第七天丢失、加号拉长 | 日期格和箭头显式尺寸，FAB 等宽高 | 390×844、320×568 七天及圆形按钮通过 |
| 七天报表虚假翻页提示 | 仅多页显示箭头、页码、指示点和滑动说明 | 390×844、320×568 七天通过 |
| 报表切换条滚动后不完整 | 切换条 sticky，修正页面 overflow 对其影响 | 390×844 滚动后保持完整 |

原生截图保存在本机 `/Users/bytedance/.codex/visualizations/2026/09/05/01a070df-0535-7db3-a721-d98902c15136/native-followup/`：subject.png、activity.png、schedule.png、calendar.png、reports-seven.png、reports-scrolled.png，以及 subject-320.png、calendar-320.png、reports-320.png。不将包含当前家庭资料的原生截图写入仓库。

原生320活动目录名称完整，但初始画面确认按钮位于下方；滚动可达性尚未完成验证。随后 CUA 返回 noWindowsAvailable，未完成320安排与430原生检查。以上为开发者工具视觉检查，不是节点几何测量或真机验收。

## 二级交互提案

[三屏设计总览](../prototypes/DREV-20260905-UX-04/secondary-board.html) / [渲染截图](../prototypes/DREV-20260905-UX-04/screenshots/secondary-overview.png)。

- 目录使用两列、完整名称、搜索与明确选中状态；自定义名称单独一页签。
- 确认按钮固定在弹层底部，目录内部纵向滚动；未选择或输入时禁用。
- 活动安排明确区分固定/不固定，星期可多选，开始结束输入等宽；保存前显示安排摘要。
- 移除为次要操作并二次确认；关闭编辑不保存。
- 不增加后台接口，不改变复习策略、学习确认规则或现有账号数据。

CUA 实际点击：目录选择添加道德与法治、自定义陶艺、多选周三/周五保存，均在原型内存中完成。320×568、390×844、430×932 共9组合测量：根页面无横向溢出、选项无横向越界、底部操作始终处于视口中。证据见 `screenshots/secondary-geometry.json`。此证据不能替代原生验收。

## 验证与剩余项

- `npm test`：58/58 通过。初跑57/58，星期比例静态断言失败；恢复显式 aspect-ratio 约束后重跑通过。
- `node --check app.js`、`node --check secondary-flows.js`：通过。
- `uv run python tools/validate_miniprogram.py`：通过，11页，204236 bytes。
- `uv run python tools/check_architecture.py`：通过，checked=2。
- 未运行：本轮后端测试、数据库/模型真实写入、实际微信邀请、双账号审批。原因：本轮变更为布局与独立交互提案，未修改后端或调用这些写入路径。此前模型超时、线上邀请GET不兼容等未被本轮消除。
- 未完成：430原生、320剩余弹层、系统字体放大、键盘与安全区、iOS/Android真机及节点几何。保持 BUG-010 VERIFYING，不以源码或HTML检查替代这些证据。

## UX-04 原生视觉落地

产品负责人批准六页总览后，今日、日程、记录、报表、设置和家庭与成员已改为 UX-04 的暖白手册视觉。实现补齐自定义 Tab 图标和直接进入时的高亮恢复、真实功能图标、统一 18px 内容列、卡片边界、按钮双轴居中、七日完整网格、报表 100 天 30/30/30/10 分屏及家庭页自我保护。设置继续保留基础科目与账号现有附加科目/课外活动，不用演示数据覆盖数据库状态。

390×844 的 HTML 目标、原生截图和逐页并排图保存在 `/Users/bytedance/.codex/visualizations/2026/09/05/01a070df-0535-7db3-a721-d98902c15136/ux04-native/`。原生截图由微信开发者工具 automator 获取；实际日期、当前时刻和账号数据造成的状态差异已单独识别，不计为布局偏差。最终验证为前端测试、lint、小程序静态校验、后端单元/集成/契约测试和架构检查；详细数量以本次最终重跑输出为准。
